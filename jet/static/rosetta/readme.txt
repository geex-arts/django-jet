This css files are original admin css files, which are overriden by django-jet (empty).
Rosetta uses this files, so we added them here, and also have overriden roseta base.html
template to set correct static paths.
